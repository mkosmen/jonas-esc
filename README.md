# Project: Simple E-commerce Shopping Cart
## Laravel 12 starter kit (vue)

- Run the following commands in order

```php
composer install

npm install
npm run build

composer run dev

php artisan migrate
php artisan db:seed
```
